import React from "react";
import ReactDOM from "react-dom/client";
import TimeTrackrHero from "./components/TimeTrackrHero.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TimeTrackrHero />
  </React.StrictMode>
);
