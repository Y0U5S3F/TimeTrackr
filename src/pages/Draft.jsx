// The original public/draft.html was an empty (0-byte) placeholder file —
// linked from the nav but with no implemented content yet. Preserved as-is.
export default function Draft() {
  return null;
}
